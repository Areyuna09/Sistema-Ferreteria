<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1" fx:controller="com.ferreteria.controllers.ProductsController" styleClass="dashboard-root">

    <!-- Navbar Superior -->
    <top>
        <HBox styleClass="navbar" alignment="CENTER_LEFT" spacing="12">
            <padding>
                <Insets top="0" right="32" bottom="0" left="32"/>
            </padding>

            <!-- Logo y Marca -->
            <HBox alignment="CENTER_LEFT" spacing="10">
                <Label text="🔧" styleClass="navbar-icon"/>
                <Label text="Ferreteria" styleClass="navbar-brand"/>
            </HBox>

            <Region HBox.hgrow="ALWAYS"/>

            <!-- Menu de Navegacion -->
            <HBox alignment="CENTER" spacing="6">
                <Button text="Dashboard" onAction="#handleBack" styleClass="nav-button"/>
                <Button text="Productos" styleClass="nav-button nav-button-active"/>
                <Button text="Ventas" onAction="#handleSales" styleClass="nav-button"/>
                <Button text="Reportes" onAction="#handleReports" styleClass="nav-button"/>
                <Button text="Usuarios" onAction="#handleUsers" styleClass="nav-button"/>
            </HBox>

            <Region HBox.hgrow="ALWAYS"/>

            <!-- Usuario y Logout -->
            <HBox alignment="CENTER_RIGHT" spacing="20">
                <VBox alignment="CENTER_RIGHT">
                    <Label fx:id="welcomeLabel" text="Administrador" styleClass="nav-user-name"/>
                    <Label fx:id="roleLabel" text="Admin" styleClass="nav-user-role"/>
                </VBox>
                <Button text="Cerrar Sesion" onAction="#handleLogout" styleClass="logout-btn, danger"/>
            </HBox>
        </HBox>
    </top>

    <!-- Contenido Principal -->
    <center>
        <ScrollPane fitToWidth="true" fitToHeight="true" styleClass="scroll-pane">
            <VBox styleClass="main-content" spacing="28">
                <padding>
                    <Insets top="32" right="40" bottom="32" left="40"/>
                </padding>

                <!-- Header de Pagina -->
                <HBox alignment="CENTER_LEFT" spacing="20">
                    <VBox HBox.hgrow="ALWAYS" spacing="6">
                        <Label text="Gestión de Productos" styleClass="page-title"/>
                        <Label text="Administra el inventario de productos de tu ferretería" styleClass="page-subtitle"/>
                    </VBox>
                    <Label fx:id="dateLabel" styleClass="date-label"/>
                </HBox>

                <!-- Barra de búsqueda y botones -->
                <VBox spacing="16">
                    <HBox alignment="CENTER_LEFT" spacing="16">
                        <TextField fx:id="searchField" prefWidth="400" promptText="🔍 Buscar producto por ID, código, nombre, categoría o ubicación..." styleClass="search-field" HBox.hgrow="ALWAYS"/>
                        <Button mnemonicParsing="false" onAction="#handleNewProduct" styleClass="action-button, success" text="+ Nuevo Producto" />
                        <Button mnemonicParsing="false" onAction="#handleRefresh" styleClass="action-button, primary" text="🔄 Recargar" />
                    </HBox>
                </VBox>

                <!-- Tabla de productos -->
                <TableView fx:id="productsTable" VBox.vgrow="ALWAYS" styleClass="full-width-table">
                    <columns>
                       <TableColumn fx:id="idColumn" prefWidth="60.0" text="ID" />
                       <TableColumn fx:id="codeColumn" prefWidth="100.0" text="Código" />
                       <TableColumn fx:id="nameColumn" prefWidth="250.0" text="Nombre" />
                       <TableColumn fx:id="categoryColumn" prefWidth="150.0" text="Categoría" />
                       <TableColumn fx:id="locationColumn" prefWidth="120.0" text="Ubicación" />
                       <TableColumn fx:id="priceColumn" prefWidth="120.0" text="Precio" />
                       <TableColumn fx:id="stockColumn" prefWidth="80.0" text="Stock" />
                       <TableColumn fx:id="actionsColumn" prefWidth="150.0" text="Acciones" />
                    </columns>
                    <columnResizePolicy>
                        <TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/>
                    </columnResizePolicy>
                </TableView>

            </VBox>
        </ScrollPane>
    </center>

</BorderPane>