<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<HBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.ferreteria.controllers.NavbarController"
      styleClass="navbar" alignment="CENTER_LEFT" spacing="12">
    <padding>
        <Insets top="0" right="32" bottom="0" left="32"/>
    </padding>

    <!-- Logo y Marca -->
    <HBox alignment="CENTER_LEFT" spacing="10">
        <Label text="🔧" styleClass="navbar-icon"/>
        <Label text="Ferreteria" styleClass="navbar-brand"/>
    </HBox>

    <Region HBox.hgrow="ALWAYS"/>

    <!-- Menu de Navegacion -->
    <HBox alignment="CENTER" spacing="6">
        <Button fx:id="btnDashboard" text="Dashboard" onAction="#handleDashboard" styleClass="nav-button"/>
        <Button fx:id="btnProductos" text="Productos" onAction="#handleProducts" styleClass="nav-button"/>
        <Button fx:id="btnCategorias" text="Categorías" onAction="#handleCategories" styleClass="nav-button"/>
        <Button fx:id="btnVentas" text="Ventas" onAction="#handleSales" styleClass="nav-button"/>
        <Button fx:id="btnReportes" text="Reportes" onAction="#handleReports" styleClass="nav-button"/>
        <Button fx:id="btnSettings" text="Configuración" onAction="#handleSettings" styleClass="nav-button"/>
        <Button fx:id="btnUsuarios" text="Usuarios" onAction="#handleUsers" styleClass="nav-button"/>
    </HBox>

    <Region HBox.hgrow="ALWAYS"/>

    <!-- Usuario y Logout -->
    <HBox alignment="CENTER_RIGHT" spacing="20">
        <VBox alignment="CENTER_RIGHT">
            <Label fx:id="userNameLabel" text="Usuario" styleClass="nav-user-name"/>
            <Label fx:id="userRoleLabel" text="Rol" styleClass="nav-user-role"/>
        </VBox>
        <Button text="Cerrar Sesion" onAction="#handleLogout" styleClass="logout-btn danger"/>
    </HBox>
</HBox>
